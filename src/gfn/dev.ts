// Functions only used during development mode 



export const emit_m = function (e, M, INFO) {
    // send a message to window
    // so that anyone can listen to it

    // const user = GFN.AS.user || {}; // can't convert undefined or null to object
    // const ev = new CustomEvent("emit_m", {detail: {M : M || {}, INFO : INFO || {}, user : user || {}}});
    // window.dispatchEvent(ev);
}